This is the last step of the plan, so your scope is wider than an ordinary step.

- Finish your assigned step first.
- Then verify the overall goal against the whole plan — don't just reason that it should hold. If the goal describes a workflow a client will run against what you built (clone, push, curl, connect, invoke the program), run that exact workflow yourself now and confirm it succeeds end to end. This is the last check before the work is verified.
- Fix a gap only when a check actually shows one. Do not rewrite, reformat, or re-save files that are already correct — retyping a working file can corrupt it, and an unneeded edit is a new bug, not a safeguard.
- This closes gaps within this final step; it does not change the plan.
